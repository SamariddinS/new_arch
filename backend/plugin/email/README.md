## Parameter Configuration

Uses local email configuration by default

Supports dynamic configuration of email parameters through the `config plugin`. When the dynamic configuration `EMAIL_STATUS` is set to `1`, the dynamic configuration will be automatically applied
